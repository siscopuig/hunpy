Process items() -> each item can contain multiple iframe sources
	- Create an Advert instance
	- Process iframe sources -> loop
		- Check if src is already invalid
		- Check if src is data:source
		- Check if src is a know adserver domain AND item is a know placement
		- Check that src is not invalid
			Check that src do not contain invalid patterns
			Check that domain is not in ignored domain list
			Check that src has a valid request response
	Set advert


Notes:

    23-10-2018

    - Create an Image Searcher
    - Revise click on element (current method does open more than one tab very often)
    - Find images from main document
        - create an image instance
        - find all images using .//img
            - from each img element get attributes (src, onclick, style)
            - from each img element find parent ("./ancestor::a")
                - if parent(<a>) get attributes(href, onclick, style)

        - get element location

    - Find url from attributes (onclick, style)
        - onclick: "EBG.ads["1075102305_6674910311817199"].onImageClick("1075102305_6674910311817199", true,"ebDefaultImg_1075102305_6674910311817199", "https://www.clearbridge.com/global-esg.html?cmpid=cbieu18_eur_web_penage_ros_728x90_wtr", "", "")"
        - style: 'background-image: http://...'


    - Validation process
        - validate container size
        - validate






